{
  "name": "nestjs-swagger-api-implicit-queries-decorator",
  "version": "1.0.0",
  "description": "NestJS Swagger decorator for multiple implicit queries",
  "main": "./dist/index",
  "types": "./dist/index",
  "scripts": {
    "prepublishOnly": "rimraf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aluisiora/nestjs-swagger-api-implicit-queries-decorator.git"
  },
  "keywords": [
    "nestjs",
    "swagger",
    "queries"
  ],
  "author": "Aluisio Rodrigues Amaral",
  "license": "MIT",
  "devDependencies": {
    "@types/lodash": "^4.*",
    "@types/node": "^8.10.32",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "@nestjs/swagger": "^2.*",
    "lodash": "^4.*"
  }
}
